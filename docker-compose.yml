version: '3.8'

services:
  36kr-rss:
    build: .
    container_name: 36kr-rss
    ports:
      - "5000:5000"
    volumes:
      - ./history.json:/app/history.json
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
